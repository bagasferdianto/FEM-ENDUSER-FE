import { Metadata } from "next"

export const metadata : Metadata = {
    title : `Voting`,
    description : `This is voting page`
}

export default function Voting () {
    return <h1>Ini adalah halaman Voting atau Page Voting ygy</h1>
}